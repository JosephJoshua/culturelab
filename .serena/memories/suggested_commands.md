# Suggested commands
- Install deps (if needed): `npm install`
- Dev server: `npm run dev`
- Build: `npm run build`
- Start production build: `npm run start`
- Lint (Biome): `npm run lint` (biome check)
- Format (Biome): `npm run format`
- Clean Next cache (if needed): `rm -rf .next`
